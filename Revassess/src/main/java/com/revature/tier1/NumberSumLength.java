package com.revature.tier1;

public class NumberSumLength {

	public static boolean checkNumberPowerLength(long num) {
		int sum = 0;
		int powerLength = String.valueOf(num).length();

		int total1 = (int)Math.pow(num,powerLength);
		if(num /10 >0)
		{

		}

		return false;
	}
}
